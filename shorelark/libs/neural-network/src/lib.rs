#[cfg(test)]

pub struct Network{
    layers: Vec<Layer>;
}


struct Layer;
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}

impl Network{
    pub fn propagate(&self, inputs: Vec<f32>) -> Vec<f32>{
        todo!()
    }
}


int get_berry_number(){

}

fn berry_number() -> usize {
    return ();
}
